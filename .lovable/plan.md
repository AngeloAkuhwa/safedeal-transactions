## Goal\nMake `/admin/transactions/:transactionId` fully database-driven via a single admin-only edge function that returns the exact response shape you specified.\n\n## 1. Edge Function — `supabase/functions/admin-transaction-detail/index.ts` (rewrite)\n\nAuth & security:\n- Require `Authorization: Bearer …`. Return `401` if missing/invalid.\n- Validate user via `userClient.auth.getUser(token)`.\n- Check admin role via `rpc(\